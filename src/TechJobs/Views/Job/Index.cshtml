@{
// TODO #2.1 - DONE - declare the ViewModel (outside of this @{ } block)
}

@model TechJobs.Models.Job

<table class="job-listing">
    @{ 
// TODO #2.2 - DONE - display the job fields, one per row (outside of this @{ } block)
    }
    <tr>
        <th>Name</th>
        <td>@Model.Name</td>
    </tr>
    <tr>
        <th>Employer</th>
        <td>@Model.Employer</td>
    </tr>
    <tr>
        <th>Location</th>
        <td>@Model.Location</td>
    </tr>
    <tr>
        <th>Skill</th>
        <td>@Model.CoreCompetency</td>
    </tr>
    <tr>
        <th>Position Type</th>
        <td>@Model.PositionType</td>
    </tr>
</table>